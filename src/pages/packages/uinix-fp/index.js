import React from 'react';

import NavPageLayout from '../../../layouts/nav-page-layout.js';

const links = [];

const Page = () => {
  return <NavPageLayout links={links} />;
};

export default Page;
