import {themeMapping} from 'uinix-theme';

import {Json} from '../../../system/components/index.js';

# Theme Mapping

This document allows easy reference of the **uinix-theme** theme mapping, a mapping of theme properties to CSS properties, which is used to formalize the **uinix-theme** theme spec.

<Json value={themeMapping} />
