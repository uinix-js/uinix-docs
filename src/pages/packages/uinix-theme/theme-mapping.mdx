import {themeMapping} from 'uinix-theme';

import {Json, PackageLink} from '../../../system/components/index.js';

# Theme Mapping

This document provides easy reference for the <PackageLink name="uinix-theme" /> theme mapping, a mapping of theme properties to CSS properties, which is used to formalize the <PackageLink name="uinix-theme" /> theme spec.

<Json value={themeMapping} />
